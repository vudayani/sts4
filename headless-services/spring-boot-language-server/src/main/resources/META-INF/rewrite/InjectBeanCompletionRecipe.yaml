type: specs.openrewrite.org/v1beta/recipe
name: com.vmware.tanzu.spring.framework.InjectBeanCompletionRecipe
displayName: Inject bean completions
description: >
  Automates the injection of a specified bean into Spring components by adding the necessary field and import, creating the constructor if it doesn't exist, and injecting the bean as a constructor parameter.
tags:
  - spring-boot
recipeList:
  - org.springframework.ide.vscode.commons.rewrite.java.AddFieldRecipe:
      fullyQualifiedName: "{{fullyQualifiedName}}"
      ignoreDefinition: true
  - org.springframework.ide.vscode.commons.rewrite.java.ConstructorInjectionRecipe:
      fullyQualifiedName: "{{fullyQualifiedName}}"
      fieldName: "{{fieldName}}"
      classFqName: "{{classFqName}}"
      ignoreDefinition: true